// STYLES
import styles from './Filters.module.css';

const Filters = () => {
    
    return (
        <div className={ styles.container }>
            
        </div>
    );
}

export default Filters;